rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function isAdmin() {
      return isSignedIn() &&
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isAdmin == true;
    }

    match /users/{userId} {
      allow read: if isOwner(userId) || isAdmin();
      allow create: if isOwner(userId);
      // Admins may only toggle another user's `isAdmin` flag (see UsersTable.tsx),
      // never rewrite the rest of that user's profile.
      allow update: if isOwner(userId) ||
        (isAdmin() && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['isAdmin']));
      allow delete: if isOwner(userId);

      match /{document=**} {
        allow read, write: if isOwner(userId);
      }
    }

    // --- Shared Expense Spaces ---
    // A space's Firestore document ID IS its join code. Looking a space up by
    // code is therefore a get()-by-ID, never a where()+list query — this is the
    // fix for the join/list circularity bug that killed two prior attempts at
    // this feature (see git history: e601f255, 80a8d0ef, 8b4d1dbb, 83bcbda1, 33299a02).
    function isSharedMember(spaceId) {
      return isSignedIn() &&
        exists(/databases/$(database)/documents/sharedSpaces/$(spaceId)/members/$(request.auth.uid));
    }

    function isSpaceOwner(spaceId) {
      return isSignedIn() &&
        get(/databases/$(database)/documents/sharedSpaces/$(spaceId)).data.ownerId == request.auth.uid;
    }

    match /sharedSpaces/{spaceId} {
      // Any signed-in user may look up a space by its join code — a get()-by-ID
      // only. `list` is fully closed, so codes can never be enumerated.
      allow get: if isSignedIn();
      allow list: if false;
      allow create: if isSignedIn() && request.resource.data.ownerId == request.auth.uid;
      allow update: if isSharedMember(spaceId) && resource.data.ownerId == request.auth.uid;
      allow delete: if resource.data.ownerId == request.auth.uid;

      match /members/{userId} {
        // isSharedMember lets existing members read each other's membership doc
        // (member list, "paid by" lookups). isOwner(userId) covers a get()-by-ID
        // (e.g. checking your own membership before mounting the rest of a
        // space's data). The "list my spaces" collection-group query filters on
        // the `uid` FIELD (not the path segment) — Firestore's `list` rule
        // provability requires the rule to reference the same field the query
        // filters on, so resource.data.uid == request.auth.uid is what actually
        // makes that query work.
        allow read: if isSharedMember(spaceId) || isOwner(userId) || resource.data.uid == request.auth.uid;
        allow create: if isOwner(userId) && request.resource.data.uid == userId;
        allow update: if isOwner(userId);
        allow delete: if isOwner(userId) || isSpaceOwner(spaceId);
      }

      match /expenses/{expenseId} {
        allow read, write: if isSharedMember(spaceId);
      }
    }
  }
}
