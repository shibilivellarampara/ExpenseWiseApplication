/**
 * @fileoverview Firestore Security Rules for Expense Tracker App
 *
 * Core Philosophy:
 * This ruleset enforces a strict user-ownership model. Each user has a dedicated data tree under `/users/{userId}`.
 * Only the authenticated user can read and write data within their own tree.
 *
 * Data Structure:
 * All user-specific data (profiles, expenses, categories, etc.) is nested under the `/users/{userId}` collection.
 * This structure ensures that data is isolated and access is controlled on a per-user basis.
 *
 * Key Security Decisions:
 * - User listing is disallowed to prevent unauthorized access to user IDs.
 * - All write operations require the user to be authenticated and the target document to belong to the authenticated user.
 *
 * Denormalization for Authorization:
 * No denormalization is explicitly used in this ruleset. All authorization checks are based on the user ID in the path.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Secure access to the user profiles. Only the authenticated user can read and write their own profile.
     * @path /users/{userId}
     * @allow (create) - User with ID 'user123' creates their own profile with matching ID.
     * @allow (get) - User with ID 'user123' reads their own profile.
     * @allow (update) - User with ID 'user123' updates their own profile.
     * @allow (delete) - User with ID 'user123' deletes their own profile.
     * @deny (create) - User with ID 'user456' attempts to create a profile for user 'user123'.
     * @deny (get) - User with ID 'user456' attempts to read profile of user 'user123'.
     * @principle Enforces document ownership for writes.
     */
    match /users/{userId} {
      function isSignedIn() {
        return request.auth != null;
      }

      function isOwner(userId) {
        return request.auth.uid == userId;
      }

      function isExistingOwner(userId) {
        return isOwner(userId) && resource != null;
      }

      allow get: if isOwner(userId);
      allow list: if false;
      allow create: if isSignedIn() && isOwner(userId) && request.resource.data.id == userId;
      allow update: if isExistingOwner(userId) && request.resource.data.id == resource.data.id;
      allow delete: if isExistingOwner(userId);
    }

    /**
     * @description Secure access to user-specific expenses. Only the authenticated user can read and write their own expenses.
     * @path /users/{userId}/expenses/{expenseId}
     * @allow (create) - User with ID 'user123' creates an expense under their profile.
     * @allow (get) - User with ID 'user123' reads their own expense.
     * @allow (update) - User with ID 'user123' updates their own expense.
     * @allow (delete) - User with ID 'user123' deletes their own expense.
     * @deny (create) - User with ID 'user456' attempts to create an expense for user 'user123'.
     * @deny (get) - User with ID 'user456' attempts to read expense of user 'user123'.
     * @principle Restricts access to a user's own data tree.
     */
    match /users/{userId}/expenses/{expenseId} {
      function isSignedIn() {
        return request.auth != null;
      }

      function isOwner(userId) {
        return request.auth.uid == userId;
      }

       function isExistingOwner(userId) {
        return isOwner(userId) && resource != null;
      }

      allow get: if isOwner(userId);
      allow list: if isOwner(userId);
      allow create: if isSignedIn() && isOwner(userId) && request.resource.data.userId == userId;
      allow update: if isExistingOwner(userId) && request.resource.data.userId == resource.data.userId;
      allow delete: if isExistingOwner(userId);
    }

    /**
     * @description Secure access to user-specific contributions. Only the authenticated user can read and write their own contributions.
     * @path /users/{userId}/contributions/{contributionId}
     * @allow (create) - User with ID 'user123' creates a contribution under their profile.
     * @allow (get) - User with ID 'user123' reads their own contribution.
     * @allow (update) - User with ID 'user123' updates their own contribution.
     * @allow (delete) - User with ID 'user123' deletes their own contribution.
     * @deny (create) - User with ID 'user456' attempts to create a contribution for user 'user123'.
     * @deny (get) - User with ID 'user456' attempts to read contribution of user 'user123'.
     * @principle Restricts access to a user's own data tree.
     */
    match /users/{userId}/contributions/{contributionId} {
      function isSignedIn() {
        return request.auth != null;
      }

      function isOwner(userId) {
        return request.auth.uid == userId;
      }

       function isExistingOwner(userId) {
        return isOwner(userId) && resource != null;
      }

      allow get: if isOwner(userId);
      allow list: if isOwner(userId);
      allow create: if isSignedIn() && isOwner(userId) && request.resource.data.userId == userId;
      allow update: if isExistingOwner(userId) && request.resource.data.userId == resource.data.userId;
      allow delete: if isExistingOwner(userId);
    }

    /**
     * @description Secure access to user-specific categories. Only the authenticated user can read and write their own categories.
     * @path /users/{userId}/categories/{categoryId}
     * @allow (create) - User with ID 'user123' creates a category under their profile.
     * @allow (get) - User with ID 'user123' reads their own category.
     * @allow (update) - User with ID 'user123' updates their own category.
     * @allow (delete) - User with ID 'user123' deletes their own category.
     * @deny (create) - User with ID 'user456' attempts to create a category for user 'user123'.
     * @deny (get) - User with ID 'user456' attempts to read category of user 'user123'.
     * @principle Restricts access to a user's own data tree.
     */
    match /users/{userId}/categories/{categoryId} {
      function isSignedIn() {
        return request.auth != null;
      }

      function isOwner(userId) {
        return request.auth.uid == userId;
      }

      function isExistingOwner(userId) {
        return isOwner(userId) && resource != null;
      }

      allow get: if isOwner(userId);
      allow list: if isOwner(userId);
      allow create: if isSignedIn() && isOwner(userId) && request.resource.data.userId == userId;
      allow update: if isExistingOwner(userId) && request.resource.data.userId == resource.data.userId;
      allow delete: if isExistingOwner(userId);
    }

    /**
     * @description Secure access to user-specific payment methods. Only the authenticated user can read and write their own payment methods.
     * @path /users/{userId}/paymentMethods/{paymentMethodId}
     * @allow (create) - User with ID 'user123' creates a payment method under their profile.
     * @allow (get) - User with ID 'user123' reads their own payment method.
     * @allow (update) - User with ID 'user123' updates their own payment method.
     * @allow (delete) - User with ID 'user123' deletes their own payment method.
     * @deny (create) - User with ID 'user456' attempts to create a payment method for user 'user123'.
     * @deny (get) - User with ID 'user456' attempts to read payment method of user 'user123'.
     * @principle Restricts access to a user's own data tree.
     */
    match /users/{userId}/paymentMethods/{paymentMethodId} {
      function isSignedIn() {
        return request.auth != null;
      }

      function isOwner(userId) {
        return request.auth.uid == userId;
      }

      function isExistingOwner(userId) {
        return isOwner(userId) && resource != null;
      }

      allow get: if isOwner(userId);
      allow list: if isOwner(userId);
      allow create: if isSignedIn() && isOwner(userId) && request.resource.data.userId == userId;
      allow update: if isExistingOwner(userId) && request.resource.data.userId == resource.data.userId;
      allow delete: if isExistingOwner(userId);
    }

    /**
     * @description Secure access to user-specific tags. Only the authenticated user can read and write their own tags.
     * @path /users/{userId}/tags/{tagId}
     * @allow (create) - User with ID 'user123' creates a tag under their profile.
     * @allow (get) - User with ID 'user123' reads their own tag.
     * @allow (update) - User with ID 'user123' updates their own tag.
     * @allow (delete) - User with ID 'user123' deletes their own tag.
     * @deny (create) - User with ID 'user456' attempts to create a tag for user 'user123'.
     * @deny (get) - User with ID 'user456' attempts to read tag of user 'user123'.
     * @principle Restricts access to a user's own data tree.
     */
    match /users/{userId}/tags/{tagId} {
      function isSignedIn() {
        return request.auth != null;
      }

      function isOwner(userId) {
        return request.auth.uid == userId;
      }

      function isExistingOwner(userId) {
        return isOwner(userId) && resource != null;
      }

      allow get: if isOwner(userId);
      allow list: if isOwner(userId);
      allow create: if isSignedIn() && isOwner(userId) && request.resource.data.userId == userId;
      allow update: if isExistingOwner(userId) && request.resource.data.userId == resource.data.userId;
      allow delete: if isExistingOwner(userId);
    }
  }
}