/**
 * @fileoverview Firestore Security Rules for the Expense Tracker application.
 *
 * Core Philosophy:
 * This ruleset enforces a strict user-ownership model. Each user has full control
 * over their own data, and no access to other users' data. All data is nested
 * under /users/{userId}.
 *
 * Data Structure:
 * The database is structured hierarchically:
 * - /users/{userId}: User profile information.
 * - /users/{userId}/expenses/{expenseId}: Expense records for a given user.
 * - /users/{userId}/accounts/{accountId}: Financial account information for a given user.
 * - /users/{userId}/contributions/{contributionId}: Contribution records for shared expenses for a given user.
 * - /users/{userId}/categories/{categoryId}: Expense categories defined by the user.
 * - /users/{userId}/tags/{tagId}: Expense tags defined by the user.
 *
 * Key Security Decisions:
 * - User listing is disallowed.
 * - All data is private and requires authentication.
 * - Data validation is relaxed to allow for rapid prototyping, except for fields
 *   critical for authorization (e.g., userId).
 *
 * Denormalization for Authorization:
 *  - All documents within a user's data tree include a `userId` field,
 *    allowing for simple `isOwner()` checks without additional reads.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Enforces user-ownership for UserProfile documents.
     * @path /users/{userId}
     * @allow (create) A user can create their own profile if the userId in the path matches their auth UID.
     * @allow (get, list, update, delete) A user can only access their own profile.
     * @deny (create) A user cannot create a profile with a userId that does not match their auth UID.
     * @principle Enforces document ownership and prevents unauthorized data access.
     */
    match /users/{userId} {
      function isSignedIn() {
        return request.auth != null;
      }

      function isOwner(userId) {
        return isSignedIn() && request.auth.uid == userId;
      }

      // For updates and deletes, check that the document exists and is owned by the user.
      function isExistingOwner(userId) {
        return isOwner(userId) && resource != null;
      }

      allow get: if isOwner(userId);
      allow list: if false; // User listing is disallowed

      // Allow creating a user profile if the userId matches the authenticated user's ID.
      allow create: if isOwner(userId) && request.resource.data.id == userId;

      //Enforce immutability of the userId field.
      allow update: if isExistingOwner(userId) && request.resource.data.id == resource.data.id;
      allow delete: if isExistingOwner(userId);
    }

    /**
     * @description Enforces user-ownership for Expense documents.
     * @path /users/{userId}/expenses/{expenseId}
     * @allow (create) A user can create an expense if the userId in the path matches their auth UID and the expense's userId field.
     * @allow (get, list, update, delete) A user can only access their own expenses.
     * @deny (create) A user cannot create an expense with a userId that does not match their auth UID.
     * @principle Enforces document ownership and prevents unauthorized data access.
     */
    match /users/{userId}/expenses/{expenseId} {
      function isSignedIn() {
        return request.auth != null;
      }

      function isOwner(userId) {
        return isSignedIn() && request.auth.uid == userId;
      }

      // For updates and deletes, check that the document exists and is owned by the user.
      function isExistingOwner(userId) {
        return isOwner(userId) && resource != null;
      }

      allow get: if isOwner(userId);
      allow list: if isOwner(userId);

      // Allow creating an expense if the userId matches the authenticated user's ID.
      allow create: if isOwner(userId) && request.resource.data.userId == userId;

      //Enforce immutability of the userId field.
      allow update: if isExistingOwner(userId) && request.resource.data.userId == resource.data.userId;
      allow delete: if isExistingOwner(userId);
    }

    /**
     * @description Enforces user-ownership for Account documents.
     * @path /users/{userId}/accounts/{accountId}
     * @allow (create) A user can create an account if the userId in the path matches their auth UID and the account's userId field.
     * @allow (get, list, update, delete) A user can only access their own accounts.
     * @deny (create) A user cannot create an account with a userId that does not match their auth UID.
     * @principle Enforces document ownership and prevents unauthorized data access.
     */
    match /users/{userId}/accounts/{accountId} {
      function isSignedIn() {
        return request.auth != null;
      }

      function isOwner(userId) {
        return isSignedIn() && request.auth.uid == userId;
      }

      // For updates and deletes, check that the document exists and is owned by the user.
      function isExistingOwner(userId) {
        return isOwner(userId) && resource != null;
      }


      allow get: if isOwner(userId);
      allow list: if isOwner(userId);
      // Allow creating an account if the userId matches the authenticated user's ID.
      allow create: if isOwner(userId) && request.resource.data.userId == userId;

      //Enforce immutability of the userId field.
      allow update: if isExistingOwner(userId) && request.resource.data.userId == resource.data.userId;
      allow delete: if isExistingOwner(userId);
    }

    /**
     * @description Enforces user-ownership for Contribution documents.
     * @path /users/{userId}/contributions/{contributionId}
     * @allow (create) A user can create a contribution if the userId in the path matches their auth UID and the contribution's userId field.
     * @allow (get, list, update, delete) A user can only access their own contributions.
     * @deny (create) A user cannot create a contribution with a userId that does not match their auth UID.
     * @principle Enforces document ownership and prevents unauthorized data access.
     */
    match /users/{userId}/contributions/{contributionId} {
      function isSignedIn() {
        return request.auth != null;
      }

      function isOwner(userId) {
        return isSignedIn() && request.auth.uid == userId;
      }

      // For updates and deletes, check that the document exists and is owned by the user.
      function isExistingOwner(userId) {
        return isOwner(userId) && resource != null;
      }

      allow get: if isOwner(userId);
      allow list: if isOwner(userId);
      // Allow creating a contribution if the userId matches the authenticated user's ID.
      allow create: if isOwner(userId) && request.resource.data.userId == userId;

      //Enforce immutability of the userId field.
      allow update: if isExistingOwner(userId) && request.resource.data.userId == resource.data.userId;
      allow delete: if isExistingOwner(userId);
    }

    /**
     * @description Enforces user-ownership for Category documents.
     * @path /users/{userId}/categories/{categoryId}
     * @allow (create) A user can create a category if the userId in the path matches their auth UID and the category's userId field.
     * @allow (get, list, update, delete) A user can only access their own categories.
     * @deny (create) A user cannot create a category with a userId that does not match their auth UID.
     * @principle Enforces document ownership and prevents unauthorized data access.
     */
    match /users/{userId}/categories/{categoryId} {
      function isSignedIn() {
        return request.auth != null;
      }

      function isOwner(userId) {
        return isSignedIn() && request.auth.uid == userId;
      }

      // For updates and deletes, check that the document exists and is owned by the user.
      function isExistingOwner(userId) {
        return isOwner(userId) && resource != null;
      }

      allow get: if isOwner(userId);
      allow list: if isOwner(userId);
      // Allow creating a category if the userId matches the authenticated user's ID.
      allow create: if isOwner(userId) && request.resource.data.userId == userId;

      //Enforce immutability of the userId field.
      allow update: if isExistingOwner(userId) && request.resource.data.userId == resource.data.userId;
      allow delete: if isExistingOwner(userId);
    }

    /**
     * @description Enforces user-ownership for Tag documents.
     * @path /users/{userId}/tags/{tagId}
     * @allow (create) A user can create a tag if the userId in the path matches their auth UID and the tag's userId field.
     * @allow (get, list, update, delete) A user can only access their own tags.
     * @deny (create) A user cannot create a tag with a userId that does not match their auth UID.
     * @principle Enforces document ownership and prevents unauthorized data access.
     */
    match /users/{userId}/tags/{tagId} {
      function isSignedIn() {
        return request.auth != null;
      }

      function isOwner(userId) {
        return isSignedIn() && request.auth.uid == userId;
      }

      // For updates and deletes, check that the document exists and is owned by the user.
      function isExistingOwner(userId) {
        return isOwner(userId) && resource != null;
      }

      allow get: if isOwner(userId);
      allow list: if isOwner(userId);
      // Allow creating a tag if the userId matches the authenticated user's ID.
      allow create: if isOwner(userId) && request.resource.data.userId == userId;

      //Enforce immutability of the userId field.
      allow update: if isExistingOwner(userId) && request.resource.data.userId == resource.data.userId;
      allow delete: if isExistingOwner(userId);
    }
  }
}